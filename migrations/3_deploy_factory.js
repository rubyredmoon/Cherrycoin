const EIP20Factory =
 coverage_test
  artifacts.require('./EIP20Factory.sol');

module.exports = (deployer) => {
  deployer.deploy(EIP20Factory);
};

  artifacts.require(`./EIP20Factory.sol`)

module.exports = (deployer) => {
  deployer.deploy(EIP20Factory)
}
 1.0.0
